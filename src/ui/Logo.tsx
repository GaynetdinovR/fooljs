import styles from '@/styles/modules/Ui.module.sass';

const Logo = () => {
	return (
        <div className={styles.logo}>
			<img src="icons/logoclean.png" alt="logo" />
		</div>
	);
};

export default Logo;